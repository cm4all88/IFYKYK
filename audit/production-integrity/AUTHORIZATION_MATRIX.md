# Authorization Matrix (Phase 3)

Every API route handler: authentication method, authorization check, ownership scoping,
and whether RLS applies. Generated by `_tools/gen-authz.js`, then manually adjudicated —
the scanner is helper-aware (`resolveSpotlightProfile()` performs `getUser()` internally),
so routes that authenticate through a helper are **not** reported as unauthenticated.

**Key.** *RLS bypassed* is not a defect by itself — it is correct for webhooks, cron and
admin paths. It is a defect when it is not paired with a gate. `lib/supabase-server.ts`
`createClient()` is the anon, RLS-enforced client; `createServiceClient()` bypasses RLS.

| Route | Methods | Authentication | Authorization / ownership | RLS | Writes | Verdict |
|---|---|---|---|---|---|---|
| `/api/account/delete` | POST | getUser() | user_id = session user | BYPASSED (service role) | yes | 🟢 getUser() then service role scoped to that user |
| `/api/admin/campaigns/create` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/content` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | enforced (anon client) | no | 🟢 gated |
| `/api/admin/creators/pick` | POST, DELETE | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/creators/post` | POST, DELETE | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/creators/profile` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/creators/social-post` | POST, DELETE | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/creators/tier` | POST, PATCH, DELETE | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/new-creator-alert` | POST | getUser() | user_id = session user; admin-wide | enforced (anon client) | no | 🟢 own-profile + 10-minute freshness replay guard |
| `/api/admin/prospects` | POST, PATCH, DELETE | isAdmin() + getUser() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/prospects/build-page` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/prospects/import` | POST | isAdmin() + getUser() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/prospects/outreach` | POST, PATCH, PUT | isAdmin() + getUser() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/studio/commit` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/video-studio/analyze` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | yes | 🟢 gated |
| `/api/admin/video-studio/caption` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | enforced (anon client) | no | 🟢 gated |
| `/api/admin/video-studio/creator/[id]` | GET | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | no | 🟢 gated |
| `/api/admin/video-studio/creators` | GET | isAdmin() + getUser() via isAdmin() | admin-wide | BYPASSED (service role) | no | 🟢 gated |
| `/api/admin/video-studio/hooks` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | enforced (anon client) | no | 🟢 gated |
| `/api/admin/video-studio/script` | POST | isAdmin() + getUser() via isAdmin() | admin-wide | enforced (anon client) | no | 🟢 gated |
| `/api/advisor` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/advisor/bio` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/advisor/signup` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/analytics` | GET | getUser() | user_id = session user | enforced (anon client) | no | 🟢 getUser() + own profile; plan cap enforced server-side |
| `/api/auth/signout` | POST | **NONE** | — | enforced (anon client) | no | 🟠 unauthenticated read — review |
| `/api/billing` | GET, POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/billing/portal` | POST | getUser() | user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/billing/setup` | POST | getUser() | user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/blocks` | GET, POST, DELETE | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/campaigns/assist` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/campaigns/donate` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/claim` | POST | **NONE** | — | BYPASSED (service role) | yes | 🟢 unauth by design; atomic single-use claim, format-validated. Well built |
| `/api/comments` | GET, POST, DELETE | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/comments/boost` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/cron/billing-dunning` | GET | CRON_SECRET bearer | — | enforced (anon client) | yes | 🟠 **SL-039** fails open if CRON_SECRET unset |
| `/api/cron/live-billing` | GET | CRON_SECRET bearer | — | enforced (anon client) | yes | 🟠 **SL-039** fails open if CRON_SECRET unset |
| `/api/cron/publish-scheduled` | GET | CRON_SECRET bearer | — | enforced (anon client) | yes | 🟠 **SL-039** fails open if CRON_SECRET unset |
| `/api/digital/download` | GET | **NONE** | — | BYPASSED (service role) | yes | 🟡 unauth by design (token is the credential); token never expires |
| `/api/digital/purchase` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/digital/upload` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/download/[token]` | GET | **NONE** | — | BYPASSED (service role) | yes | 🔴 **SL-004** unauth, service role, all 3 guards disabled |
| `/api/early-access` | POST | getUser() | fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/email/welcome` | POST | getUser() | user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/fan/interests` | GET, POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/fan/me` | GET | getUser() | fan_user_id = session user | enforced (anon client) | no | 🟢 getUser(), all queries scoped to session user (queries broken — SL-016) |
| `/api/feed` | GET | getUser() | user_id = session user; fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/feed/discover` | GET | getUser() | user_id = session user; fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/gift-subscription` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/gift-subscription/redeem` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/live/chat` | POST, GET | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/live/end` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/live/heartbeat` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/live/start` | POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/live/tip` | POST, GET | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/live/whip` | POST, DELETE | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/marketplace` | GET, POST, PATCH | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/import/action` | POST | getUser() via resolveSpotlightProfile() | creator_profile_id = own profile; own spotlight profile | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/import/capture` | POST | getUser() via resolveSpotlightProfile() | own spotlight profile | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/import/csv` | POST | getUser() via resolveSpotlightProfile() | own spotlight profile | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/import/drafts` | GET | getUser() via resolveSpotlightProfile() | creator_profile_id = own profile; own spotlight profile | enforced (anon client) | no | 🟢 gated |
| `/api/marketplace/import/photos` | POST | getUser() via resolveSpotlightProfile() | own spotlight profile | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/import/screenshots` | POST | getUser() via resolveSpotlightProfile() | own spotlight profile | enforced (anon client) | yes | 🟢 gated |
| `/api/marketplace/purchase` | POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/medals/award` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/medals/balance` | GET | getUser() | fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/medals/confirm` | POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/medals/purchase` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/merch` | GET | **NONE** | — | enforced (anon client) | no | 🟢 public catalogue read, no sensitive columns |
| `/api/merch/catalog` | GET | **NONE** | — | enforced (anon client) | no | 🟠 unauthenticated read — review |
| `/api/merch/checkout` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/merch/create-product` | POST, PATCH | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | yes | 🟢 gated |
| `/api/merch/mockup` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/merch/shipping-quote` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/merch/status` | GET | **NONE** | — | enforced (anon client) | no | 🟠 unauthenticated read — review |
| `/api/messages/front-row` | POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/notifications` | GET, POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/onboarding` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/posts/archive` | POST | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | yes | 🟢 gated |
| `/api/posts/delete` | POST | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | yes | 🟢 gated |
| `/api/posts/like` | POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/posts/pin` | POST | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | yes | 🟢 gated |
| `/api/posts/publish` | POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/posts/tags` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/posts/unlock` | POST | getUser() | fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/posts/vod` | POST | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | yes | 🟢 gated |
| `/api/profile/me` | GET | getUser() | user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/recommendations` | GET | getUser() | fan_user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/referrals/attribute` | POST | **NONE** | — | BYPASSED (service role) | no | 🟠 unauthenticated; RPC has self/dup guards so impact is limited |
| `/api/referrals/creator` | POST | **NONE** | — | BYPASSED (service role) | yes | 🔴 **SL-005** unauthenticated, service role, issues $29 credits |
| `/api/referrals/me` | GET | getUser() | — | enforced (anon client) | no | 🟢 getUser(), passes user.id to RPC (but see SL-045 grant) |
| `/api/referrals/stats` | GET | getUser() | user_id = session user; creator_profile_id = own profile | enforced (anon client) | no | 🟢 getUser() + own profile |
| `/api/referrals/subscriber` | POST | **NONE** | — | BYPASSED (service role) | yes | 🔴 unauthenticated, service role, forges referral rows (SL-043) |
| `/api/referrals/verify` | POST | getUser() | — | enforced (anon client) | no | 🟢 getUser(), passes user.id to RPC (but see SL-045 grant) |
| `/api/search` | GET | **NONE** | — | enforced (anon client) | no | 🟢 public search, no sensitive columns |
| `/api/social-addbacks` | GET, POST, DELETE | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/social-addbacks/orders` | GET, POST | getUser() | — | BYPASSED (service role) | yes | 🟠 service role without an explicit gate |
| `/api/social-addbacks/purchase` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/social-posts` | GET, POST, DELETE | getSession() | — | enforced (anon client) | yes | 🟠 **SL-062** getSession() not getUser() |
| `/api/social-posts/backfill` | POST | CRON_SECRET bearer | — | BYPASSED (service role) | yes | 🟢 gated |
| `/api/social-posts/fetch-oembed` | POST | **NONE** | — | enforced (anon client) | no | 🟠 unauthenticated read — review |
| `/api/stripe/connect/refresh` | POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/stripe/connect/return` | GET | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/stripe/connect/session` | POST | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/stripe/connect/start` | POST, GET | getUser() | user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/studio/build` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/subscribe` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/subscribe/tier` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/subscription` | POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/subscription/cancel` | POST | getUser() | fan_user_id = session user | enforced (anon client) | yes | 🟢 gated |
| `/api/super-tip` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/tiers` | GET, POST, PATCH, DELETE | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/tiers/assist` | POST | **NONE** | — | enforced (anon client) | no | 🔴 **SL-064** unauthenticated Anthropic spend |
| `/api/tip` | POST | getUser() | — | enforced (anon client) | no | 🟠 **SL-022** notifies creator before payment; unauthenticated by design |
| `/api/track/presence` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/unsubscribe` | POST | **NONE** | — | BYPASSED (service role) | yes | 🟢 unauth by design; HMAC-signed token (lib/email.ts) |
| `/api/upload` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/upload-video` | POST | getUser() | — | enforced (anon client) | no | 🟢 gated |
| `/api/watermark` | POST | getUser() | user_id = session user | enforced (anon client) | no | 🟢 gated |
| `/api/webhooks/auth` | POST | webhook signature/secret | — | enforced (anon client) | no | 🟢 SUPABASE_WEBHOOK_SECRET header, fails closed |
| `/api/webhooks/ccbill` | POST | webhook signature/secret | — | BYPASSED (service role) | yes | 🟡 verify signature handling against CCBill docs |
| `/api/webhooks/printful` | POST | webhook signature/secret | — | BYPASSED (service role) | yes | 🟠 **SL-040** secret in query string; keyed off own order id |
| `/api/webhooks/stripe` | POST | webhook signature/secret | — | BYPASSED (service role) | yes | 🔴 **SL-006** signature verified but no replay window |
| `/api/wishlist/confirm` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |
| `/api/wishlist/purchase` | POST | getUser() | — | enforced (anon client) | yes | 🟢 gated |

---

## Summary

| Metric | Count |
|---|---|
| Route files | 124 |
| Handlers (method × route) | 155 |
| Uses `getUser()` directly or via helper | 95 |
| Uses `getSession()` only | 1 |
| Webhook signature / shared secret | 4 |
| CRON_SECRET gated | 4 |
| `isAdmin()` gated | 18 |
| Service role (RLS bypassed) | 27 |
| **No authentication of any kind** | **20** |

---

## Pages, layouts and server actions

| Surface | Guard | Verdict |
|---|---|---|
| `app/admin/**` (17 pages) | `app/admin/layout.tsx` calls `await isAdmin()` then `notFound()` | 🟢 Every admin page inherits the layout guard. The scanner flag on `app/admin/page.tsx` and `app/admin/video-studio/page.tsx` is a **false positive** — verified by reading the layout. |
| `app/(platform)/**` | `app/(platform)/layout.tsx` — no user → `redirect('/login')` | 🟢 Authentication only. Ownership is re-checked per page via `creator_profiles.user_id = user.id`. |
| `app/[creator]/**` | none — public by design | 🟢 Correct; paid-content gating is per-component and server-rendered. |
| `app/claim/[code]/page.tsx` | none — the code is the credential | 🟢 Renders a form; `/api/claim` performs the actual authorization. |
| `middleware.ts` | session lookup, admin gate, creator-vs-fan routing | 🟢 Routing only. Per CLAUDE.md it is **not** authorization, and every page re-checks. Matcher covers `/`, `/dashboard`, `/onboarding`, `/feed`, `/account`, `/admin/:path*`. |
| Server actions (`"use server"`) | re-check inside the action | 🟢 `app/admin/comms/page.tsx` re-runs `isAdmin()` inside the action, as CLAUDE.md requires. |

## IDOR testing

Each class was tested by tracing whether a request-supplied identifier can reach a query without
being intersected with the session identity.

| Test | Result |
|---|---|
| Creator A reads/modifies Creator B | **No IDOR found in routes.** Every creator route resolves the profile by `user_id = user.id` and filters by that resolved `profile.id` — it never trusts a body-supplied `creator_profile_id`. `app/api/marketplace/import/action/route.ts:31` is the model: `.eq('id', id).eq('creator_profile_id', profile.id)`. **But** SL-001/012/013 allow exactly this against PostgREST directly, bypassing routes entirely. |
| Fan A reads/modifies Fan B | **No IDOR in routes** — `/api/fan/me` scopes every query to `fan_user_id = uid`. **Yes via RLS**: SL-003 (fabricate a purchase) and SL-029 (`tips` publicly readable). |
| Non-admin reaching admin routes | **No.** All `/api/admin/*` routes return 403 via `isAdmin()`; all admin pages are covered by the layout. |
| Admin status trusted from client data | **No.** `isAdmin()` derives from `auth.getUser().email` server-side. The hardcoded `ADMIN_ID` in `components/site-header.tsx:5` and `dashboard/page.tsx:56` gates **UI only** — but see SL-033 for the three-way divergence. |
| Unverified Stripe webhook accepted | **No** — the signature is verified. **But it is replayable forever** (SL-006). |
| Referral code credits the wrong account | **Yes** — SL-005. The referrer is taken from an unauthenticated request body. |
| Public route exposes email / Stripe ids / addresses / private posts | **Yes, via RLS rather than via routes**: `merch_orders` shipping addresses (SL-012), `tips` fan ids and messages (SL-029), `creator_billing` Stripe ids (SL-001), `gift_subscriptions.recipient_email`, `marketplace_orders.buyer_email`, `social_addback_orders.fan_email` (SL-013). |
| Service-role client imported into browser code | **No.** `createServiceClient()` appears only in `route.ts` files, `lib/` server modules and server components. No `"use client"` file imports it, and `SUPABASE_SERVICE_ROLE_KEY` is never exposed under a `NEXT_PUBLIC_` name. |
| CSRF / origin protection | **Absent, but low impact.** Supabase auth cookies are `SameSite=Lax`, which blocks cross-site POST. `/api/auth/signout` is the only state-changing unauthenticated POST without a token; worst case is a forced logout. |

## Service-role usage review

`createServiceClient()` appears in 12 routes. Each was checked for a compensating gate.

| Route | Gate | Verdict |
|---|---|---|
| `/api/webhooks/stripe` | signature | 🟢 correct use (replay caveat, SL-006) |
| `/api/webhooks/printful` | shared secret + keyed off own `loudcap_order_id` | 🟢 correct use (SL-040 on transport) |
| `/api/webhooks/auth` | `SUPABASE_WEBHOOK_SECRET` header | 🟢 correct use |
| `/api/claim` | none by design — the code is the credential, format-validated before any DB work | 🟢 correct use, well reasoned in comments |
| `/api/account/delete` | `getUser()` first, then scoped to that user | 🟢 correct use |
| `/api/digital/download` | none — the token is the credential | 🟡 acceptable; the token never expires |
| `/api/download/[token]` | none — and all three guards are disabled | 🔴 **SL-004** |
| `/api/referrals/creator` | none | 🔴 **SL-005** |
| `/api/referrals/subscriber` | none | 🔴 forges referral rows (SL-043) |
| `/api/referrals/attribute` | none; the RPC carries its own guards | 🟠 acceptable — guards live in `record_referral` |
| `/api/unsubscribe` | HMAC-signed token (`lib/email.ts`) | 🟢 correct use |
| `/api/social-addbacks/orders` | `getUser()` first | 🟢 correct use |

## RLS reality check

Per the audit rules, no route was marked safe merely because RLS exists. Every applicable policy
was read. Results are in `FINDINGS.md` SL-001, 002, 003, 011, 012, 013, 029, 055, 056:

- **20 policies grant write access to `PUBLIC`** (anon + authenticated) because they omit a `TO`
  clause. No policy in the repository is `AS RESTRICTIVE`, so these OR with — and therefore
  defeat — the correct owner-scoped policies sitting beside them.
- **13 policies grant unrestricted read** to `PUBLIC`.
- **9 tables have no RLS at all**, including `creator_profiles`.
- **6 tables have RLS with zero policies** — deny-all, which is correct here since only the
  service role writes them.

**Route-level authorization is broadly sound. Database-level authorization is not — and the
database is directly reachable with the public anon key, so the routes are not the perimeter.**
